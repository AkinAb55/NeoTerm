/* uKernel hamis <linux/signal.h> — a net-usb driverek includolják, de nem
 * hasznalnak belole semmit; a valodi kernel-fejlec elfedese (sigset_t utkozes). */
#ifndef _UK_LINUX_SIGNAL_H
#define _UK_LINUX_SIGNAL_H
#include <linux/types.h>
#include <linux/sched.h>
#endif
